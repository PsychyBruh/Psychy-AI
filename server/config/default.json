{
    "openaiApiKey": "${OPENAI_API_KEY}",
    "serpApiKey": "${SERP_API_KEY}",
    "jwtSecret": "${JWT_SECRET}",
    "dbPath": "${DB_PATH:-./emerald.db}",
    "modelPath": "../llama-models/mistral-7b-instruct-v0.1.Q5_0.gguf",
    "llama": {
      "temp": 0.7,
      "n_predict": 64,
      "confidenceThreshold": -1.0 
    }
  }
